=encoding utf8

=head1 子程序

Perl是门很松散的语言，定义一个子程序只要传入数据列表就行了，然后自己去处理这个列表，这样灵活轻巧，不过有时也因此容易出错。

Perl里的sub不用提前定义，我们运行时动态新建，也可以在之后重新定义。sub本身也可以用来构造其他sub，而每一个子程序都能包含各自独立的私有数据。定义完的sub可以保存为一个引用，然后像其他数据一样作为参数传递。

善用活用Perl sub，复杂问题也会变简单。

=head2 条款目录


